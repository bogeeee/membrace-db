Simple example that opens the database (db/db.json) and adds a user to it

### Run it
```bash
git clone https://github.com/bogeeee/membrace-db.git
cd membrace-db/example
npm install --ignore-scripts
npm run dev
```
